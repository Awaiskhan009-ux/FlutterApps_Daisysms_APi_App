name: flutter_application_2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.0
  cloud_firestore: ^6.0.3
  firebase_auth: ^6.1.1 
  provider: ^6.0.5 
  shared_preferences: ^2.1.1

  
  # iOS-style icons
  cupertino_icons: ^1.0.8

  # 👇 For smooth page indicators in onboarding
  smooth_page_indicator: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 👇 Ye folder-level path hai — isme jitni bhi images hongi sab use ho sakengi
  assets:
    - assets/images/
